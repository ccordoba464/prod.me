export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type Track = {
  id: string;
  title: string;
  artist: string;
  genre: string;
  year: number;
  duration: string;
  key: string;
  bpm: number;
};
